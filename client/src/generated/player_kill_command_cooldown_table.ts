// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { PlayerKillCommandCooldown } from "./player_kill_command_cooldown_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `player_kill_command_cooldown`.
 *
 * Obtain a handle from the [`playerKillCommandCooldown`] property on [`RemoteTables`],
 * like `ctx.db.playerKillCommandCooldown`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.playerKillCommandCooldown.on_insert(...)`.
 */
export class PlayerKillCommandCooldownTableHandle {
  tableCache: TableCache<PlayerKillCommandCooldown>;

  constructor(tableCache: TableCache<PlayerKillCommandCooldown>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<PlayerKillCommandCooldown> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `player_id` unique index on the table `player_kill_command_cooldown`,
   * which allows point queries on the field of the same name
   * via the [`PlayerKillCommandCooldownPlayerIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.playerKillCommandCooldown.player_id().find(...)`.
   *
   * Get a handle on the `player_id` unique index on the table `player_kill_command_cooldown`.
   */
  player_id = {
    // Find the subscribed row whose `player_id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: Identity): PlayerKillCommandCooldown | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.player_id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: PlayerKillCommandCooldown) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: PlayerKillCommandCooldown) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: PlayerKillCommandCooldown) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: PlayerKillCommandCooldown) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: PlayerKillCommandCooldown, newRow: PlayerKillCommandCooldown) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: PlayerKillCommandCooldown, newRow: PlayerKillCommandCooldown) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
