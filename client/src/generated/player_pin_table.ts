// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { PlayerPin } from "./player_pin_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `player_pin`.
 *
 * Obtain a handle from the [`playerPin`] property on [`RemoteTables`],
 * like `ctx.db.playerPin`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.playerPin.on_insert(...)`.
 */
export class PlayerPinTableHandle {
  tableCache: TableCache<PlayerPin>;

  constructor(tableCache: TableCache<PlayerPin>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<PlayerPin> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `player_id` unique index on the table `player_pin`,
   * which allows point queries on the field of the same name
   * via the [`PlayerPinPlayerIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.playerPin.player_id().find(...)`.
   *
   * Get a handle on the `player_id` unique index on the table `player_pin`.
   */
  player_id = {
    // Find the subscribed row whose `player_id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: Identity): PlayerPin | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.player_id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: PlayerPin) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: PlayerPin) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: PlayerPin) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: PlayerPin) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: PlayerPin, newRow: PlayerPin) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: PlayerPin, newRow: PlayerPin) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
