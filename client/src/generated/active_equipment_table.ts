// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { ActiveEquipment } from "./active_equipment_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `active_equipment`.
 *
 * Obtain a handle from the [`activeEquipment`] property on [`RemoteTables`],
 * like `ctx.db.activeEquipment`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.activeEquipment.on_insert(...)`.
 */
export class ActiveEquipmentTableHandle {
  tableCache: TableCache<ActiveEquipment>;

  constructor(tableCache: TableCache<ActiveEquipment>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<ActiveEquipment> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `player_identity` unique index on the table `active_equipment`,
   * which allows point queries on the field of the same name
   * via the [`ActiveEquipmentPlayerIdentityUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.activeEquipment.player_identity().find(...)`.
   *
   * Get a handle on the `player_identity` unique index on the table `active_equipment`.
   */
  player_identity = {
    // Find the subscribed row whose `player_identity` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: Identity): ActiveEquipment | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.player_identity, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: ActiveEquipment) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: ActiveEquipment) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: ActiveEquipment) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: ActiveEquipment) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: ActiveEquipment, newRow: ActiveEquipment) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: ActiveEquipment, newRow: ActiveEquipment) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
