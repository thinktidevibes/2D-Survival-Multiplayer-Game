// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { PlayerCorpseDespawnSchedule } from "./player_corpse_despawn_schedule_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `player_corpse_despawn_schedule`.
 *
 * Obtain a handle from the [`playerCorpseDespawnSchedule`] property on [`RemoteTables`],
 * like `ctx.db.playerCorpseDespawnSchedule`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.playerCorpseDespawnSchedule.on_insert(...)`.
 */
export class PlayerCorpseDespawnScheduleTableHandle {
  tableCache: TableCache<PlayerCorpseDespawnSchedule>;

  constructor(tableCache: TableCache<PlayerCorpseDespawnSchedule>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<PlayerCorpseDespawnSchedule> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `corpse_id` unique index on the table `player_corpse_despawn_schedule`,
   * which allows point queries on the field of the same name
   * via the [`PlayerCorpseDespawnScheduleCorpseIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.playerCorpseDespawnSchedule.corpse_id().find(...)`.
   *
   * Get a handle on the `corpse_id` unique index on the table `player_corpse_despawn_schedule`.
   */
  corpse_id = {
    // Find the subscribed row whose `corpse_id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): PlayerCorpseDespawnSchedule | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.corpse_id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: PlayerCorpseDespawnSchedule) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: PlayerCorpseDespawnSchedule) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: PlayerCorpseDespawnSchedule) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: PlayerCorpseDespawnSchedule) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: PlayerCorpseDespawnSchedule, newRow: PlayerCorpseDespawnSchedule) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: PlayerCorpseDespawnSchedule, newRow: PlayerCorpseDespawnSchedule) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
